# 강사 참고 스크립트 — 학생은 열지 않아도 됩니다

이 폴더의 `.py` 파일은 **강사가 교재 자료를 만들 때 쓴 것**입니다.
수업에서 실행하지 않고, 실습 PC에 Python 이 없어도 됩니다.

| 파일 | 무엇을 만들었나 |
|---|---|
| `build_news.py` · `news_reference_builder.py` | M2 뉴스 저장본과 참고 리포트 |
| `build_macro.py` · `reference_checks.py` | M3 데이터 정리·검사 참고본 |
| `news_rates_reference.py` · `test_news_rates_reference.py` | 요구 R-01~R-04 의 Python 참고 구현과 검사 |

학생이 쓰는 것은 **Claude 앱(Code 탭)과 브라우저뿐**입니다.
같은 검사를 브라우저에서 보려면 `framework_reference/browser_lab.html` 을 더블클릭하세요.
