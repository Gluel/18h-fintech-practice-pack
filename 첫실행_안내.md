# 첫 실행 안내 (실습 PC · Windows + Claude 데스크톱 앱)

> 그림과 함께 보시려면 **`guides/01_첫실행.html`** 을 여세요. 이 문서는 같은 내용의 글 판입니다.

## 0. 먼저 알아두실 것
- 이 PC는 **전원을 끄거나 재부팅하면 저장 파일과 로그인이 모두 지워집니다.** 매 모듈 끝에 GitHub에 올립니다.
- 계좌·카드번호·주민번호·비밀번호·API 키는 **어떤 화면에도 입력하지 않습니다.**
- 공용 PC입니다. 로그인할 때 「로그인 상태 유지」를 끄고, 퇴실 전에 로그아웃합니다.

## 1. 파일부터 열기
1. 패들렛 「실습 환경 세팅」에서 **M1_start.zip** 을 바탕화면에 내려받습니다.
2. ZIP을 **오른쪽 클릭 → 모두 압축 풀기**. ZIP 안에서 바로 열면 수정이 저장되지 않습니다.
3. 만들어진 **M1_start 폴더**를 엽니다. `index.html` · `docs` · `data` 가 보이면 됩니다.
4. `index.html` 을 두 번 눌러 브라우저로 엽니다. 표와 차트가 보이면 성공입니다. (인터넷이 없어도 열립니다)

## 2. Claude 데스크톱 앱에서 폴더 열기
1. 바탕화면의 **Claude** 아이콘을 두 번 누릅니다.
2. 로그인 화면이 나오면 **본인 이메일**로 「이메일로 계속하기」 → 메일의 인증 링크를 누릅니다.
   - 메일이 안 오면 **스팸함**을 보세요. 다른 사람이 대신 눌러 주면 본인 기기 인증이 실패합니다.
3. 앱에서 **폴더 열기**를 누르고 방금 압축을 푼 **M1_start** 폴더를 고릅니다.

> 터미널이나 `cd`, `python3` 명령을 쓰지 않습니다. 실습 PC에 Python이 없을 수 있습니다.

## 3. 첫 요청 — 읽기만 시킵니다

```
이 폴더에 뭐가 있는지 설명해줘. 수정하지 말고 읽은 파일을 알려줘.
```

읽은 파일 이름이 답으로 나오고, 파일은 바뀌지 않습니다.

## 4. 한 곳만 바꿔 보기

```
제목을 「내 금융 지표 보기」로 바꿔줘. 다른 곳은 바꾸지 마.
```

바꾼 뒤 브라우저에서 **새로고침(F5)** 합니다. **제목만** 바뀌고 나머지는 그대로여야 합니다.
여기까지가 M1 Lv1 완료입니다.

## 5. 막히면
`guides/05_막혔을때.html` — 로그인 풀림 · 폴더를 못 찾음 · 화면이 안 바뀜 · 없는 내용을 지어냄 등 7가지.
