# 모듈별 실습 가이드 (데스크톱 앱 기준)

> 그림 판: **`guides/04_모듈별_실습.html`** · 완성 예시: **`kit/reference/index.html`**

## 공통 규칙
- 요청은 **작게**. 바꿀 파일과 범위를 지정합니다. (사용량도 아끼고 확인도 쉽습니다)
- 만든 뒤에는 **브라우저 새로고침**으로 직접 확인합니다. 「완료했습니다」는 증거가 아닙니다.
- 데이터는 **`data/*.js`** 로 읽습니다. HTML에서 `fetch` 로 CSV를 읽으면 파일을 더블클릭해 열 때 막힙니다.
- 차트는 **외부 라이브러리·CDN 없이 SVG** 로 그립니다. 인터넷이 끊기면 빈 화면이 됩니다.

## M2 · 뉴스 리포트
강사 저장본(`M2_news/snapshot/`)으로 시작합니다. 직접 받을 때는 **딱 1번만** 요청합니다.

```
M2_news/snapshot/bok_2026-09-11_nobody.html에서 제목, 전체 주소(https://…), 발행일(YYYY-MM-DD), 출처를
5건만 표로 보여줘. 웹에 새로 요청하지 마. 확인 후 my_project/news_items.json에
title, url, date, source 키로 저장해. 본문·발췌가 없으므로 5건 모두 요약을 보류해.
```

확인이 끝나면:

```
확인한 my_project/news_items.json으로 my_project/report.html을 만들어줘. 외부 CSS·JS·폰트 없이 한 파일로.
상단에 수집일·수집 URL·건수, 하단에 「출처 · 원문 전문 미포함 · 투자 판단 근거 아님」.
본문 요약은 모두 「제목만 확보 — 요약 보류」, 관련 지표 칸은 「M3에서 채움」으로 둬.
빈 목록을 넣으면 자료 없음을 보여주고 가짜 기사를 만들지 마.
```

## M3 · 데이터 받기와 점검

```
my_project/data/stock_sample.csv 에 대해 행 수, 첫 3행, 마지막 날짜,
빈 값 개수(빈칸과 점(.) 모두), 최솟값·최댓값을 표로 보여줘. 원본은 수정하지 마.
```

화면으로 넘길 때:

```
같은 내용을 data/stock_sample.js (window.DATA_STOCK = {source, unit, rows}) 로 만들어줘.
행 형식은 [date, close]야. CSV 첫 행 date,close 는 헤더이고 관측 행에 넣지 마.
숫자를 옮겨 적지 말고 감싸기만 해. HTML에서는 fetch 대신 <script src="data/stock_sample.js"> 로 읽어.
```

장기금리도 `data/kr_ltir.js`의 `window.DATA_KR_LTIR`로 읽습니다. M2에서 만든 표와 연결합니다.

```
my_project/news_items.json의 date에서 YYYY-MM을 읽고 my_project/data/kr_ltir.js의 관측월과 연결해.
my_project/report.html의 관련 지표 칸에 값(소수점 2자리 %)·관측월·OECD 출처를 보여줘.
2026-08 뉴스 4건은 4.29%, 2026-09 뉴스 1건은 관측이 없어 「미공개」야.
임의로 0이나 직전 월 값을 채우지 마. 제목·URL·발행일·출처 5행은 그대로 유지해.
```

> 한글 CSV(공공데이터포털)는 **「cp949로 읽어」** 를 덧붙입니다.

## M4 · 차트
설계부터 합니다.

```
아직 구현하지 마. data/ 와 report.html 을 읽고 화면 설계안 3개를 표로 보여줘.
칸은 5개, 각 한 줄: ①쓰는 사람·답할 질문 ②차트 종류·이유 ③x/y축·단위·기간
④빈 값일 때 모습·출처 위치 ⑤참고 UI. 내가 번호만 말하면 그 안으로 만들어줘.
```

```
고른 안으로 만들어줘. 외부 라이브러리·CDN 없이 SVG로 그리고 데이터는 data/*.js 에서 읽어.
계산은 페이지 JavaScript가 하게 하고(네가 암산해 적지 마), 계산값 1개를 「검산용」 표로 보여줘.
```

## M5 · 구현
`kit/planning/` 의 양식을 채운 뒤 요구사항 하나씩 구현합니다.

```
my_project/docs/requirements.md 의 R-01 만 구현해줘.
고칠 수 있는 파일은 my_project/index.html이야. data/는 읽기만 하고 요구사항을 바꾸지 마.
끝나면 바꾼 파일 목록과 확인 방법을 알려줘.
```

## M6 · 검사와 배포
`kit/reference/test.html` 처럼 브라우저만으로 도는 검사를 만듭니다.

```
index.html에 계산 함수가 들어 있으면 그 함수를 app.js로 옮기고 index.html에서 불러오게 해줘.
허용 파일은 index.html, app.js, test.html이야. 기존 화면 결과를 먼저 확인한 뒤 app.js 함수를 검사해.
test.html은 더블클릭으로 열리고,
내 docs/requirements.md의 각 R-ID마다 입력·기대값을 적은 검사 1개를 만들어줘.
기본 상태 3개면 n/3, 계산 R-04를 추가했으면 n/4로 실제 검사 수를 표시해.
공통 참고본은 상태 3개와 계산 1개를 합해 4/4를 확인해.
iframe·fetch·Node·Python 금지.
```

> 옛 판의 `python3 build_*.py` 절차는 쓰지 않습니다. 실습 PC에 Python이 없을 수 있습니다.
> 그 스크립트들은 **강사 참고용**으로만 남겨 둡니다.
